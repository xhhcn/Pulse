services:
  pulse:
    image: pulse:local-test
    container_name: pulse-monitor
    ports:
      - "8008:8008"
    volumes:
      - pulse-data:/app/data
    restart: unless-stopped

volumes:
  pulse-data:
